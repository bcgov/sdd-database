# Components (`src/components`)

## Search (`src/components/Search`)
- `Search`
- `FilterTags`
- `SearchResultsList`
- `SearchResultItem` — represents each individual search result card

## Entity Forms (`src/components/Entity_Forms`)
- `EmployeeForm` — used for both add + edit employee
- `OfficeForm`

## Shared / Modal
- `ModalDialog` — used for both add + edit modals
- `EditModal` — used for both offices + employees
- `DeleteAlertDialog`